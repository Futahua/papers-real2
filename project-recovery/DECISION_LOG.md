# Decision log

Preserve REAL2, its patch, the broker branch, and Gate A evidence. Do not merge without independent review or rely on `networkAccess:false` as isolation. Keep offline-required work fail-closed, approval acceptance disabled, cancellation claims conservative, and future control behind a provider-independent adapter.

The broker prompt was executed enough to produce a pushed four-commit feature branch; it must be preserved. That does not supersede Gate A FAIL or make Codex trusted. Open decisions include merge/refactor, outer isolation, Job Objects, alternative adapter, schema, approval acceptance, and cancellation verification.
