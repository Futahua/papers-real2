# Local paths

`REAL2` is protected and durable. The broker worktree, relay clone, and probe evidence are existing protected inputs. `_agent_papers_complete_recovery_20260711` is the durable recovery worktree. `_agent_papers_patch_verify_20260711` is disposable verification. The patch export is local-only but retained. GitHub branches and tags are remote-backed.
